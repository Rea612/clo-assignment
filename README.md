- Borrow create react project from scratch from [react offical documentation](https://react.dev/learn/build-a-react-app-from-scratch).
- run `npm install` then `npm run-script dev` to start the server at port 3000
- run  `npm test` to excute test
- implemented all required functions and partial optional function (add test, use typescript)

